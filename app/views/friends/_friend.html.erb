
<div class="card my-5 " style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title"><%= friend.first_name %> <%= friend.last_name %></h5>
    <p class="card-text">
      <%= friend.email %> || 
      <%= friend.phone %> ||
      <%= friend.twitter %>
    </p>
  </div>
</div>